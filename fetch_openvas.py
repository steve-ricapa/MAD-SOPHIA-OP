import sys


def main() -> None:
    raise SystemExit(
        "fetch_openvas.py is legacy and disabled. Use runtime artifacts or current XOC/AWS tooling instead."
    )


if __name__ == "__main__":
    sys.stdout.reconfigure(encoding="utf-8")
    main()
